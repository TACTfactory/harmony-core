package ${bundle_namespace}.platform.android;

import ${bundle_namespace}.platform.${bundle_name?cap_first}Adapter;
import com.tactfactory.harmony.platform.android.AndroidAdapter;


/**
 * TODO : Javadoc
 * Android adapter generated for ${bundle_name} Bundle.
 */
public class ${bundle_name?cap_first}AdapterAndroid
        extends AndroidAdapter implements ${bundle_name?cap_first}Adapter {
    
}
