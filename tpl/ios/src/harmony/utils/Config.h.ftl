<@header?interpret />

#import <Foundation/Foundation.h>

/** Host API for dev env. */
extern NSString *const REST_URL_HOST_DEV;
/** Host API for prod env. */
extern NSString *const REST_URL_HOST_PROD;

/** Url API for dev env. */
extern NSString *const REST_URL_DEV;
/** Url API for prod env. */
extern NSString *const REST_URL_PROD;

/** Url Image for dev env. */
extern NSString *const IMAGE_URL_PREFIX_DEV;
/** Url Image for prod env. */
extern NSString *const IMAGE_URL_PREFIX_PROD;

/** Url Image for dev env. */
extern NSString *const URL_SCHEME_DEV;
/** Url Image for prod env. */
extern NSString *const URL_SCHEME_PROD;

extern NSString *const URL_PATH_DEV;
extern NSString *const URL_PATH_PROD;