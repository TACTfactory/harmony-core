<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="2.5dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="2.5dp" >

    <#list fields as field>
    <TextView
        android:id="@+id/row_${name}_${field.name}"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        <#if (field_index==0)>
        android:layout_alignParentTop="true" />
		<#else>
		android:layout_below="@+id/row_${name}_${fields[field_index-1].name}" />
		</#if>
    </#list>
    <#list relations as relation>
		<#if (relation.relation_type=="@OneToOne" | relation.relation_type=="@ManyToOne")>
    <TextView
        android:id="@+id/row_${name}_${relation.name}"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        	<#if (relation_index==0)>
        android:layout_below="@+id/row_${name}_${fields?last.name}" />
			<#else>
		android:layout_below="@+id/row_${name}_${relations[relation_index-1].name}" />
			</#if>
		</#if>
    </#list>

</RelativeLayout>